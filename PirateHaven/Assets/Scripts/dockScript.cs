using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dockScript : MonoBehaviour {
/*
    public GameObject[] shipDock;
    public GameObject[] halfWay;
    public GameObject[] dockedShips;

  

	// Use this for initialization
	void Start () {


	}
	
	// Update is called once per frame
	void Update () {

        for(int i = 0; i < dockedShips.Length; i++)
        {

            if (dockedShips[i] != null)
            {

                if (dockedShips[i].GetComponent<shipController>().shipDocked == false)
                {

                    dockedShips[i] = null;

                }

            }
        }

    }

    */
}
